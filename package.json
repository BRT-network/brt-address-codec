{
  "name": "brt-address-codec",
  "version": "0.1.0",
  "description": "encodes/decodes base58 encoded BRT Ledger identifiers",
  "files": [
    "dist/*",
    "build/*"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "ISC",
  "dependencies": {
    "base-x": "3.0.8",
    "create-hash": "^1.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/BRT-network/brt-address-codec.git"
  },
  "prepublish": "tsc",
  "prepublishOnly": "tslint -p ./ && jest",
  "scripts": {
    "compile": "tsc",
    "test": "jest",
    "lint": "tslint -p ./"
  },
  "devDependencies": {
    "@types/jest": "^25.1.0",
    "@types/node": "^13.1.8",
    "jest": "^25.2.7",
    "ts-jest": "^25.0.0",
    "tslint": "^5.19.0",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.7.4"
  },
  "engines": {
    "node": ">= 10"
  }
}
